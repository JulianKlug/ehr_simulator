# CLAUDE.md

This file provides guidance to Claude Code (claude.ai/code) when working with code in this repository.

## Current state

Sessions 1-6, 9a-9c, 10, 11a-11f shipped. Python 3.11 package at `src/ehr_simulator/` with:

- **Data contract** (`ingestion/canonical.py`): four pandera schemas locking the canonical in-memory shapes (`SCALAR_TS`, `ADMISSION`, `IMAGING`, `AI_OUTPUT`).
- **Adapters** (`ingestion/`): `synthetic` (reference), `geneva` (real Geneva CSV), `mimic` (MIMIC-III CSV). Geneva + MIMIC share `_shared.py` (CSV reader, normalisation params, sidecar contract validation, categorical decoding, panel builders). `__init__.py` re-exports `load_geneva`, `GenevaDataset`, `load_mimic`, `MimicDataset`.
- **Sidecar drift gate**: `_shared.parse_normalisation_sidecar(..., check=True)` validates the columns/order/numeric content of the normalisation CSV against a frozen JSON expectation. CI re-runs both adapters' fixture builders with `--check` and fails on drift.
- **FastAPI + HTMX UI** (`web/`): `app.py` factory + lifespan, `routes.py` (timepoint slicing + per-patient routes), `panels.py` (5-state taxonomy: loading / empty-expected / empty-unexpected / partial / error), `charts.py` (plotnine SVG renderer with a11y fallback table), Jinja templates and static assets. In study mode (`serve --config/--questions`) the patient view also renders `_questions_pane.html`.
- **SQLite** (`db/`): `connection.py` (`journal_mode=WAL`, `synchronous=NORMAL`, `foreign_keys=ON`; path resolves `--db-path` → `EHR_SIM_DB_PATH` → study YAML `db_path` → `data/study_<study_id>.db` in study mode / `data/ehr_simulator.db` otherwise), `migrations.py` (nine migrations, applied in the lifespan; migration 4 creates the `study_identity` singleton table only; migration 5 adds `configuration_history`, `active_configuration` and a nullable `config_version` on the four case tables via `Migration.add_columns`, which skips columns already present so a partial apply is retry-safe; migration 6 adds `randomisation_schedules` + `randomisation_schedule_items`; migration 7 adds `schedule_id`/`case_position`/`activated_at` to `arm_assignments`, a partial unique index on `(schedule_id, case_position)` and triggers that refuse incomplete, updated or deleted `phase2_randomized` rows — `Migration.post_sql` runs after `add_columns`; migration 8 adds `case_lifecycle` with state CHECKs, terminal/no-delete triggers and a backfill of existing S11d cases; migration 9 adds `case_replacements` with activate-once/no-delete triggers), DAOs for `clinicians`, `sessions`, `arm_assignments`, `answers`, `progress`, `events`, `ingestion_issues`, `case_lifecycle`, `replacements`, `backup.py` (shutdown snapshot into `<db parent>/backups`, `--backup-dir` overrides; skipped unless the session wrote something), `cookies.py`.
- **Study identity (S11a)**: the study config (`config/study.py`) is `schema_version: "2"` and requires a `study_id` matching `^[a-z0-9][a-z0-9_-]{0,63}$` (the pattern is deliberately duplicated in `db/study_identity.py`; a lockstep test pins the two — the db layer must not import from `config`). `db/study_identity.py` is the pure SQLite API over a caller-controlled connection: `fetch(conn) -> str | None`, `bind(conn, study_id)` (already matches → no-op; different → `StudyIdentityError`; unbound + any persistent application row → refused, **no adoption flag exists**; unbound and empty → insert + commit), `require(conn, study_id)` (pure read-only, never writes — safe on read-only connections), `has_persistent_data`, `validate_study_id`. `web/app.py` lifespan calls `bind` after `apply_migrations`, before the clinician cache, and raises `StudyStartupError` (no clinician ever sees a partial boot) on `StudyIdentityError`. The CLI gates `export-answers`, `divergence-view`, and `reset-progress` with `require` before any output/mutation (a `--db-path` pointing at a mismatched or unidentified database is exit 1); `preview --html-out` binds its per-study scratch DB (`_preview_scratch_<study_id>.db`) before seeding. Identity error messages carry at most the study ids — never clinical, event, or clinician rows.
- **Configuration history (S11b)**: `db/config_history.py` is the sole writer of the append-only `configuration_history` (one row per `config_version`: hash, timestamp, description/reason, immutable study + questions JSON snapshots from `config/snapshot.py`) and the `active_configuration` singleton. `activate()` is one atomic transaction covering the study-identity bind (`study_identity.bind_in_transaction`), S11a backfill, history insert and active pointer — any failure rolls all back, leaving a fresh DB unbound; an exact repeat (same version, hash, metadata) is a pure no-op that **never moves the active pointer** (a rollback needs a new version); a reused label with different hash/metadata, a dataset change, or a foreign study is refused. The first activation on an S11a DB backfills `config_version` only when exactly one historical hash exists and matches; otherwise refused. Study-mode boot refuses without an active configuration or when the YAML hash differs (`activate-config` first). Every case is pinned via `arm_assignments` (`web/study_session.resolve_case_configuration`): existing cases use their version's snapshot (patients, timepoints, questions, gating); new cases pin the active version, and a stale server (DB active ≠ `app.state`) refuses new cases with 409. The `answers` and `progress` DAOs refuse any write/delete whose version/hash differs from the stored row (nothing mutated); session/progress/answer mismatches surface as 500 via one app-level `ConfigurationProvenanceError` handler. `build_dataset_loader(study, extra_patient_ids=...)` also loads patients already assigned in the DB, so a patient removed from the active `patient_ids` stays reachable on filtered Geneva/MIMIC datasets; the transitional index appends them after the active list.
- **Randomisation schedules (S11c)**: optional `randomisation` block on `StudyConfig` (`master_seed` 0..2^63-1, `block_length >= 1`, `block_sequence` of `start`/`other` with equal counts); omitted from serialization when absent, so pre-S11c snapshots and hashes are unchanged. `randomisation.py` holds the pure `generate_schedule` (`adaptive_block_v1`: HMAC-SHA256 over canonical JSON — never `random`/`hash()`; starting arm = less-represented across stored schedules, HMAC tie-break; arms fixed by the block pattern; each slot takes the patient with the lowest projected `|ai - no_ai|` from the **activated** allocation state, HMAC tie-break; per-item 63-bit `assignment_seed`) and the service `create_or_fetch_schedule` (identity `require`, existing schedule returned unchanged, then randomisation present, version/hash registered + matching the study snapshot + active, all under `BEGIN IMMEDIATE`). `db/randomisation.py` is the sole writer; one immutable schedule per clinician (identical re-insert → no-op, different → `RandomisationIntegrityError`). S11d changed its allocation-state argument to a loader run inside the lock (`load_activated_allocation_state`: realised `phase2_randomized` counts across clinicians and versions).
- **Explicit Start case (S11d)**: Phase 2 study mode = study mode whose **active** config has `randomisation` (`study_session.is_phase2_mode`); study mode without it keeps the `phase1_stub` GET bootstrap. `web/case_start.py` `start_next_case`: open case (a `phase2_randomized` assignment whose progress is not completed) → resume, no write; else Phase A (stale check + `create_or_fetch_schedule`, own commit — planned only) then Phase B under `BEGIN IMMEDIATE` (re-check open case + stale, first unactivated item, `require_schedule_compatible` — same seed/block settings/algorithm and the item's patient still in the active pool — then `arm_assignments.activate_planned_assignment` + session + `case.activated` + `session.start` events, one commit, rollback on any failure). `POST /case/start`: 303 / `HX-Redirect` to the case's frontier; 409 for not-Phase-2, stale server, exhausted schedule or incompatible schedule; 500 on allocation integrity. In Phase 2 the index/jumper list only the clinician's cases and offer Start / Resume / "All cases completed"; GET of an unactivated patient redirects to the index and answer/advance return 409, before anything is resolved or written. No page renders the arm, seed or schedule metadata.
- **Case lifecycle (S11e)**: optional `case_lifecycle` block on `StudyConfig` (`reconnection_grace_seconds >= MIN_RECONNECTION_GRACE_SECONDS` = 180, `voluntary_pause_enabled`, `voluntary_pause_grace_seconds` — null falls back to the reconnection grace **at use**, never in the snapshot — `target_completed_cases_per_clinician`, `max_activated_cases_per_clinician >= target`, display-only `study_target_completed_cases`); omitted from serialization when absent. States `active`/`paused`/`completed`/`incomplete` (`resumed` = the `case.resumed` transition). Layers: `db/case_lifecycle.py` (sole writer, compare-and-set transitions, a miss → `CaseLifecycleError`, nothing written) → `case_lifecycle.py` (pure `evaluate` timeout policy + uncommitted transitions) → `web/case_contact.py` (per-request gate; `app.state.clock` is the only time source — `create_app(clock=...)`, tests swap it). **Lazy timeout, no worker**: every case GET/answer/advance/heartbeat/Start runs `check` first against the policy **pinned by the case's activation config**; beyond grace → `incomplete` (`reconnection_timeout`/`pause_timeout`), session closed, `case.incomplete`, one `BEGIN IMMEDIATE` commit, then the request is refused (GET → redirect to index; POST → 409 + `HX-Redirect: /`). A clinician who never returns is caught by the operator sweep `expire-cases` (same pinned evaluation, same events). A successful contact touches `last_seen_at` after the action (a gap > `RECONNECT_GAP_SECONDS` adds `case.reconnected`). `POST /case/{pid}/heartbeat` (204; `static/heartbeat.js` every `HEARTBEAT_INTERVAL_SECONDS` = 15), `/pause` (409 when the pinned policy disables it), `/resume` (new session + `case.resumed`). A paused case GET renders only `case_paused.html` (nothing sliced) and refuses answer/advance/heartbeat. The final advance completes the case inside its S10 transaction (`case.completed`). **Open case** is now lifecycle `active`/`paused`: incomplete cases no longer block Start case; a paused one does. YAML loading refuses a `target_completed_cases_per_clinician` or `max_activated_cases_per_clinician` whose first positions in the block pattern are not 50:50 (odd limit: one-case difference); stored snapshots are not re-checked. Start case refuses (`ClinicianLimitReachedError`, 409; index `LIMIT_REACHED`) at the **active** config's limits, re-checked inside Phase B's lock; an open case stays resumable. No automatic whole-study stop. A pinned config without the section never times out, never pauses.
- **Replacement cases (S11f)**: `case_lifecycle.replacement_cases_enabled` (default false, serialized only when true so S11e hashes don't move; read from the **incomplete case's pinned** config). `replacement.py`: pure `select_replacement` = lexicographic min of `(arm ≠ original arm, clinician |ai-no_ai|, patient |ai-no_ai|, |position - next nominal|, HMAC tie rank keyed by the schedule's derived_seed_hex)` — **same arm first** is a study decision; candidates are the clinician's own unactivated, unreserved schedule items whose patient they never held and the DB's active pool still lists. `plan_replacement` (one `BEGIN IMMEDIATE` commit + `case.replacement_planned`) runs after every incomplete transition (lazy timeout, `abandon-case`), in its own commit — a failure never undoes the timeout; Start case Phase A re-plans missing ones. `db/replacements.py` is the sole writer; plans are immutable, `activated_at` set once. Start case activates the oldest pending plan before the next ordinary item through the normal S11d/S11e checks (limits, compatibility — an invalidated replacement is refused, never substituted). Index marks incomplete cases `replacement pending` / `replaced` / `no replacement`. Limits are not checked at planning.
- **Login** (`web/routes.py`): `/login` + `/logout`. The typed name is case-folded and SHA256-truncated to a 16-hex `clinician_id`, carried in an unsigned cookie; every clinician-facing route sends a cookie that misses `app.state.known_clinicians` to `/login` (303, or 200 + `HX-Redirect` for HTMX requests).
- **Answer capture** (`web/answer_capture.py`, `web/study_session.py`, `static/answers.js`): `POST /patient/{pid}/timepoint/{t_index}/answer` upserts one `answers` row per `(clinician, patient, timepoint, question)` and appends an `answer.upsert` / `answer.clear` event. The pane auto-saves on change, free-text after a 1.5s pause; an all-blank submission deletes the row so the gate reads it as unanswered. Outside study mode the route returns 409.
- **Question gating** (`web/gating.py`, `db/progress.py`, `static/advance.js`): migration 3 adds `progress`, one row per `(clinician, patient)` holding `unlocked_t_index` + `completed_at`; `unlock` is a compare-and-set on the observed frontier. `study_session.read_frontier` is the pure read the GET gate uses before `bootstrap_session` writes anything — a request past the frontier is redirected (303, or `HX-Redirect`) before `slice_to_timepoint` runs, so it cannot leak data or burn an S11 arm. `POST /patient/{pid}/timepoint/{t_index}/advance` is the one forward path: **200** advanced/finished (`HX-Push-Url` to `t+1`, or `HX-Redirect: /` after `mark_complete` + `sessions.close`), **409** blocked (the `_advance_cta.html` fragment retargeted onto itself, `advance.blocked` event, client scrolls to the first unanswered question), **412** stale (`t_index != unlocked_t_index`, or a CAS miss — answered with the frontier's view, never a write). Non-HTMX submits get POST-redirect-GET 303s. Timepoints behind the frontier render read-only and `POST /answer` refuses them with 409. Only `required: true` questions (`config/questions.py`, `StrictBool`, default `True`) block; a required multi-select must ship a `None of these` option. The index and patient jumper show per-patient progress markers and resume at each frontier.
- **CSV export** (`export.py`, S9c): `export-answers STUDY_CONFIG [--db-path P --out FILE.csv --keyfile DIR --only-complete --force]`. Pure bundle read under a WAL snapshot (`BEGIN…ROLLBACK`, never COMMIT), one row per patient × clinician × timepoint, strict persisted-value decoding via `answer_codec`, config-generation mismatch refusal (live ≠ recorded `config_hash` → exit 1), formula-injection cell guard, `clinician_id` pseudonymization with an optional mode-600 id→name keyfile (`--keyfile`). Read-only: no progress, events, or state is written.
- **Behavioral timing + divergence** (S10): `timepoint.enter`/`timepoint.exit` events (`web/timing_events.py`) — enter on the editable-frontier GET (recorded only **after** a successful render; never for blocked/frozen/completed panes) and on HTMX advance-into-next; exit (`reason` `advance|finish`) from `gating.advance()`. **A successful advance and its `timepoint.exit` — and the final `mark_complete` + `sessions.close` + event batch — are one explicit transaction** (`commit=False` on `progress.unlock`/`mark_complete`/`sessions.close`/`events.append`/`record_exit`, one `conn.commit()`, `conn.rollback()` on any failure; `write_counter` bumped once, post-commit). `timing.py` is the pure derivation (sort by `(server_ts, event_id)`; first ENTER = start; first EXIT after = end; no-enter → blank; exit-before-enter ignored; end<start → `TimingError`) plus the three wall-clock columns `timepoint_started_at`/`timepoint_ended_at`/`elapsed_seconds` appended to the export row. `divergence.py` (no `web/` imports) renders the rough per-patient divergence figure in plotnine-SVG: **every configured question renders as a facet in `questions.yaml` order** (neutral `no responses` strip when blank), then the `newly_visible_data` annotation panel (counts by canonical category — `admission · vitals · labs · other scalar · AI · imaging`; scalar rows outside `VITAL_VAR_SET`/`LAB_VAR_SET` from `ingestion/canonical.py` count as **`other scalar`**, never raw values), then the **wall-clock elapsed panel last** (fixed categorical facet order; `no completed timing intervals` strip when empty). Arm assignment rows are **config-hash validated before the clinician→arm map** (assignment-only drift → `DivergenceError`); arm → color and option → line-style are per-geom constants (deliberately **not** discrete scales, so >4 styles / >10 arms don't hit mizani palette limits and NaN). `divergence-view STUDY_CONFIG QUESTIONS --patient ID [--db-path P --out FIGURE.svg]` is the CLI. `main()` runs with `standalone_mode=True` and maps refusals to `SystemExit(1)`; a **subprocess** test pins OS status 1/0 (CliRunner can't).
- **Logging** (`logging.py`): structlog JSONL pipeline rolling at UTC midnight to `./logs/current.jsonl`.
- **Tests**: 896 tests across 37 files. Two marker suites are deselected by default: 14 Playwright tests in `tests/e2e/` (`-m e2e`) and 2 `@pytest.mark.real_data` adapter smokes that need the local Geneva + MIMIC CSVs.

Still not shipped: Geneva AI predictions (S7), Geneva real-data wired into the UI (S8) and AI-on/AI-off intervention delivery (S11g — Phase 2 arms are realised by Start case but no page varies by arm yet). See `specs/ROADMAP.md`.

Commands:

- `uv sync` — install dependencies (generates `uv.lock` on first run).
- `uv run pytest` — run the default suite (896 tests, parallelized via `pytest-xdist`; e2e + real-data deselected).
- `uv run pytest -m e2e` — run the Playwright walks (needs chromium installed).
- `uv run pytest -m real_data` — run the real-data smoke suite locally (requires Geneva + MIMIC CSVs at `.EXAMPLE_DATA_PATHS`).
- `uv run ruff check .` — lint.
- `uv run ruff format .` — format.
- `uv run ehr-simulator serve` — boot the FastAPI server at http://localhost:8000. Add `--config configs/example_config.yaml --questions configs/example_questions.yaml` for study mode (both flags or neither); `--db-path`/`--backup-dir` relocate persistence. Any of those four flags disables `--reload` (uvicorn needs the import-string entry point).
- `uv run ehr-simulator validate-config`, `validate-adapter`, `preflight`, `preview` — config and dataset checks; `preview --patient P --questions Q --html-out DIR` dumps the rendered per-timepoint HTML, questions pane included.
- `uv run ehr-simulator export-answers STUDY_CONFIG [--db-path P --out FILE.csv --keyfile DIR --only-complete --force]` — read-only CSV export (S9c); exits 1 on config-generation mismatch, missing DB, or a refused write.
- `uv run ehr-simulator divergence-view STUDY_CONFIG QUESTIONS --patient ID [--db-path P --out FIGURE.svg]` — rough per-patient divergence figure, descriptive-only (S10). Both commands have a process-exit-code contract (refusal ⇒ OS status 1, success ⇒ 0) enforced by a subprocess test (CliRunner can't).
- `uv run ehr-simulator activate-config STUDY_CONFIG QUESTIONS --version V --description D [--reason R --db-path P]` — register + activate a configuration version (S11b); required before study-mode `serve`. Exact rerun exits 0 (no-op; output names the version that stays active), any refusal exits 1.
- `uv run ehr-simulator migrate [--db-path P]` — apply pending migrations and checkpoint the WAL.
- `uv run ehr-simulator backup [--db-path P] [--backup-dir D]` — snapshot the DB (defaults `data/ehr_simulator.db` → `data/backups/`).
- `uv run ehr-simulator reset-progress STUDY_CONFIG --clinician NAME --patient PID [--to-t-index N] [--db-path P]` — operator recovery for a mis-advance: rewinds the frontier to `N` (default 0), clears `completed_at`, deletes answers strictly after `N`, records one `progress.reset` event. Unknown clinician, no walk, an index outside the study, or a completed/incomplete Phase 2 case exits 1 before any write.
- `uv run ehr-simulator abandon-case STUDY_CONFIG --clinician NAME --patient PID [--db-path P]` — mark an open Phase 2 case `incomplete` (`operator_abandoned`); terminal or unknown cases exit 1 unwritten (S11e).
- `uv run ehr-simulator expire-cases STUDY_CONFIG [--db-path P --dry-run]` — mark every open case past its pinned grace `incomplete` (`reconnection_timeout`/`pause_timeout`) and plan replacements; `--dry-run` lists them read-only. `export-answers` warns while any exist.
- `uv run ehr-simulator case-status STUDY_CONFIG [--db-path P]` — read-only per-`clinician_id` lifecycle counts and remaining limits (S11e).

CI (`.github/workflows/ci.yml`) runs `uv sync --locked`, `ruff check`, `ruff format --check`, `pytest` on Python 3.11 and 3.12, then the data-contract and fixture-sidecar drift checks, a CLI smoke (`validate-config`/`validate-adapter`/`preflight`/`preview` + a `migrate`/`backup` round-trip), and a second job for the Playwright e2e suite. The `real_data` suite runs locally only — CI has no access to the CSVs.

## What's being built

A browser-based EHR simulator that replays historical patient timeseries to a clinician at discrete timepoints, optionally displays precomputed AI model output alongside, and prompts the clinician to answer questions per timepoint. The goal is to evaluate how AI assistance changes clinician assessments and decisions. See `plan.md` for the full spec; `example_questions.md` has sample prompts.

Key architectural constraints from `plan.md` that cross multiple components and are easy to miss:

- **Timepoints are relative to first patient contact (t=0)** and configured per-study in a settings file alongside the unit (minutes/hours) and the ordered list of patient ids. The simulator reveals data up to and including the current timepoint only.
- **Data ingestion must be modular** — the simulator should accept differently-formatted input sources, not be hard-coded to one schema. Example inputs listed in `.EXAMPLE_DATA_PATHS` come from MIMIC-III and the Geneva stroke dataset.
- **For the example CSVs referenced in `.EXAMPLE_DATA_PATHS`, skip rows where the `source` column contains `"imputed"`** — only non-imputed datapoints are used.
- **AI output is consumed, not produced.** The simulator displays precomputed per-timepoint model output; it does not run models. Clinical safety features are explicitly out of scope (local-only use).
- **Question gating:** all questions for a timepoint must be answered before advancing to the next timepoint. Responses are keyed by `(patient_id, clinician_name, timepoint)` and must export to CSV with one column per question.
- **Phase 2 adds randomized AI-vs-no-AI arms per clinician-patient pair**; answer records must carry a flag indicating whether AI assistance was visible. Design data models with this in mind even if Phase 1 ships first.

## Example data

The CSVs in `.EXAMPLE_DATA_PATHS` live on the local filesystem under `/mnt/data1/klug/datasets/opsum/...` and are not in the repo. Don't assume their schema without reading them first.

## gstack

Use the `/browse` skill from gstack for all web browsing. Never use `mcp__claude-in-chrome__*` tools.

Available gstack skills: `/office-hours`, `/plan-ceo-review`, `/plan-eng-review`, `/plan-design-review`, `/design-consultation`, `/design-shotgun`, `/design-html`, `/review`, `/ship`, `/land-and-deploy`, `/canary`, `/benchmark`, `/browse`, `/connect-chrome`, `/qa`, `/qa-only`, `/design-review`, `/setup-browser-cookies`, `/setup-deploy`, `/retro`, `/investigate`, `/document-release`, `/codex`, `/cso`, `/autoplan`, `/plan-devex-review`, `/devex-review`, `/careful`, `/freeze`, `/guard`, `/unfreeze`, `/gstack-upgrade`, `/learn`.

Teammates without gstack can install it with:

```
git clone --single-branch --depth 1 https://github.com/garrytan/gstack.git ~/.claude/skills/gstack && cd ~/.claude/skills/gstack && ./setup
```

(Requires [bun](https://bun.sh).)

## Skill routing

When the user's request matches an available skill, ALWAYS invoke it using the Skill
tool as your FIRST action. Do NOT answer directly, do NOT use other tools first.
The skill has specialized workflows that produce better results than ad-hoc answers.

Key routing rules:
- Product ideas, "is this worth building", brainstorming → invoke office-hours
- Bugs, errors, "why is this broken", 500 errors → invoke investigate
- Ship, deploy, push, create PR → invoke ship
- QA, test the site, find bugs → invoke qa
- Code review, check my diff → invoke review
- Update docs after shipping → invoke document-release
- Weekly retro → invoke retro
- Design system, brand → invoke design-consultation
- Visual audit, design polish → invoke design-review
- Architecture review → invoke plan-eng-review
- Save progress, checkpoint, resume → invoke checkpoint
- Code quality, health check → invoke health

# Basics
- Never use sudo.
- Do not access files outside this repository.
- Never run production migrations.
- Ask before deleting files.
- Do not deploy or push changes.
